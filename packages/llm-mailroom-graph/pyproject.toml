[project]
name = "llm-mailroom-graph"
version = "0.1.0"
description = "Interactive graphify knowledge graph of llm-mailroom — derived static-site artifact (monorepo virtual member)"
requires-python = ">=3.11"

# Monorepo: static HTML/JSON artifact, no Python code — workspace member only
# for co-location; nothing is built or installed.
[tool.uv]
package = false
