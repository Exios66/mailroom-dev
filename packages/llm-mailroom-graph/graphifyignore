# Used when rebuilding this site from a llm-mailroom checkout:
#   cp graphifyignore /tmp/llm-mailroom/.graphifyignore
#   graphify extract /tmp/llm-mailroom --code-only --force --resolution 0.4 --exclude-hubs 99
#
# Keep the knowledge graph on the production mailroom surface.
# Tests, notebooks, and vendored assistant skills drown communities
# in fixtures and OpenRouter helper scripts — they are not the architecture.
src/tests/
notebooks/
.opencode/
docs/
deploy/
site/
data/
**/*.ipynb
**/__pycache__/
**/*.pyc
